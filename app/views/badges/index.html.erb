<h2>My Badges</h2>

<table class="table table-hover table-bordered">
  <tr style="text-align: center;">
    <th><%= t('.title') %></th>
    <th><%= t('.description') %></th>
    <th><%= t('.badge') %></th>
  </tr>

  <%= render @badges %>
</table>

<div class="mt-2">
  <%= link_to t('nav_buttons.back'), (request.referer || tests_path), class: "btn btn-primary" %>
</div>