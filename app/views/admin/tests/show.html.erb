<p>
  <strong> Test: </strong>
  <%= @test.title %>
</p>

<p>
  <strong> Level: </strong>
  <%= test_level(@test) %>
</p>

<%= link_to 'New question', new_admin_test_question_path(@test) %>

<p>
  <strong> Test question(s): <%= @test.questions.count %></strong>
  <% @test.questions.each_with_index do |question, i| %>
    <br>
    <%= "#{i + 1}. #{question.body}" %>
  <% end %>
</p>

<%= link_to 'Edit', edit_admin_test_path(@test) %> |
<%= link_to 'Back', admin_tests_path %>