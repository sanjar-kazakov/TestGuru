<h1><%= question_header(@question)%></h1>

<%= form_with model: [@test, @question] do |form| %>
<%= render 'shared/errors', resource: @question %>
<p>
  <%= form.label :question_body %><br>
  <%= form.text_field :body %>
</p>

<p> <%= form.submit %> </p>

<% end %>